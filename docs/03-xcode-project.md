# 03 — Xcode project reference

Why the project is shaped the way it is. The sources carry no comments (see
CLAUDE.md), so anything worth remembering about them lives here.

Verified working 2026-09-16 on SERV2: `BUILD SUCCEEDED`, `arm64` non-fat,
`LC_BUILD_VERSION minos 12.0 / sdk 15.2`, `MinimumOSVersion 12.0`, 12 embedded
Swift runtime dylibs, 62 MB Debug IPA.

## 1. Hand-written `project.pbxproj`

Xcode 13.2.1 has no CLI project generator. XcodeGen and Tuist would both solve it
but are third-party dependencies, against the project's zero-dependency rule, and
would require a security audit before use. The pbxproj format is stable and
readable, so it is authored directly.

The `/* Begin … section */` markers inside the pbxproj are format-mandated and
regenerated by Xcode. They are not authored comments and the no-comments rule does
not apply to them.

Object IDs are allocated from a readable block (`1A…A1` upward) rather than random
hashes. Xcode preserves them; it only mints random IDs for objects it creates.

## 2. The shared scheme is mandatory

`jellypic.xcodeproj/xcshareddata/xcschemes/jellypic.xcscheme` must stay in git.

Autocreated schemes live in `xcuserdata/`, which only exists after the project has
been opened in Xcode.app at least once. A headless `xcodebuild -scheme jellypic`
on a fresh clone or a CI runner fails outright without a *shared* scheme. This is
the classic CI trap; `.gitignore` excludes `xcuserdata/` but deliberately keeps
`xcshareddata/`.

## 3. `LaunchScreen.storyboard` is load-bearing

Not cosmetic. Without a launch storyboard iOS falls back to a legacy screen size
and runs the app **letterboxed in a 320×480-era window** on every device newer
than an iPhone 8. `UILaunchStoryboardName` in `Info.plist` points at it.

The alternative, the `UILaunchScreen` Info.plist dictionary, is iOS 14+ and
therefore unusable at this floor.

It uses a literal sRGB white rather than `systemBackgroundColor`, which is iOS 13+
and would fail to resolve on iOS 12. Consequence: the launch screen stays white in
dark mode on modern devices. Marked `LEGACY(ios12)`.

## 4. Pre-`UIScene` app lifecycle

`AppDelegate` owns a `UIWindow` directly and there is no `UIApplicationSceneManifest`
in `Info.plist`. `UISceneDelegate` is iOS 13+, so this is the only option at a 12.0
floor — and an app without a scene manifest still gets the classic window path on
every iOS through 26. Marked `LEGACY(ios12)`.

## 5. Build settings worth knowing

| Setting | Value | Why |
|---|---|---|
| `IPHONEOS_DEPLOYMENT_TARGET` | 12.0 | iPhone 5s test device |
| `ARCHS` | arm64 | iOS 11 dropped 32-bit; every iOS 12 device is arm64 |
| `TARGETED_DEVICE_FAMILY` | 1,2 | iPhone + iPad |
| `ENABLE_BITCODE` | NO | removed entirely in Xcode 14; NO is forward-compatible |
| `CODE_SIGN_STYLE` | Manual, empty identity | signing happens ad-hoc in `build.sh`, not in Xcode |
| `PRODUCT_BUNDLE_IDENTIFIER` | `com.rousseaddict.jellypic` | provisional |
| `SWIFT_VERSION` | 5.0 | |

`VALID_ARCHS` is deliberately absent everywhere: deprecated since Xcode 12 and
rejected by modern Xcode. `ARCHS` alone is authoritative.

Because the floor is below iOS 12.2 (where the Swift ABI became part of the OS),
Xcode embeds the Swift runtime into `jellypic.app/Frameworks/`. That is automatic
and needs no patching — unlike the iOS 6 apps, which swap in a Swift 5.1.5 runtime
by hand. It inflates the IPA and is why `build.sh` signs the nested dylibs before
the enclosing bundle. Raising the floor to 12.2 would make `Frameworks/` disappear.

## 6. `RootViewController` is a placeholder

It exists so the pipeline has something to compile, and prints the OS actually
running the binary — useful when the same IPA is sideloaded onto both the iOS 12
test device and the iOS 15/18/26 daily drivers. The real photo grid
(`UICollectionView`, see `02-reader-app-plan.md`) replaces it.
